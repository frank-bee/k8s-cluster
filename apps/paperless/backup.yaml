apiVersion: velero.io/v1
kind: Backup
metadata:
  name: paperless-test-backup7
  namespace: velero
spec:
  storageLocation: aws
  includedNamespaces:
    - paperless
  includedResources:
    - pods
  labelSelector:
    matchLabels:
      velerobackup: yes