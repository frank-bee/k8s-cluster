apiVersion: velero.io/v1
kind: Backup
metadata:
  name: paperless-test-backup9
  namespace: velero
spec:
  storageLocation: aws
  includedNamespaces:
    - paperless
  includedResources:
    - pods
    - persistentvolumeclaims